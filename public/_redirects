# No SPA fallback needed — Nitro Worker handles all routing via _routes.json
# The _worker.js handles API routes, SSR pages, and static assets
/admin/connections/integrations /agency/dealer-feeds 302
