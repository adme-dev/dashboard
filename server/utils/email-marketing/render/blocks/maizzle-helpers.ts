// server/utils/email-marketing/render/blocks/maizzle-helpers.ts
// Stub for the automotive hero-template / vehicles-section helpers that
// html-block.ts references for dynamic (OEM/dealer) blocks. Those dynamic
// blocks are out of scope for the agency email module, so these return empty
// markup — html-block's generic raw-HTML path is unaffected.

export function generateHeroTemplateHtml(..._args: unknown[]): string {
  return ''
}

export function generateMaizzleVehiclesSectionHtml(..._args: unknown[]): string {
  return ''
}
