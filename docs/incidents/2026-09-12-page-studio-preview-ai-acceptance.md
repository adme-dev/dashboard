# Staging AI acceptance schema mismatch — 12 September 2026

The hosted editor generated a correct synthetic contact form, but accepting it returned502. Sandbox trace records the control API returning400. Cloudflare readback identifies Dashboard preview deployment `1b291f6e-e235-4fa0-bd54-8db350e318b4`, source `062ff5c42e336c1274e0523ea7d4ea94fb74f29f`. That source uses a strict acceptance schema without `expectedCheckpointId`; the new Sandbox requires and sends it. Its old receipt also omits the acknowledged/current-head fields now required by the editor.

This staging-only repair begins at that exact deployed preview revision and backports only the acceptance schema and transactional acceptance function already present in current main `9d6b1c9a1a00b682658f87417fa9efc221a24093`. It preserves the preview's other features. Do not merge this preview branch wholesale into main: the surrounding preview tree contains unfinished work. Production remains unchanged. No migration is required.

Acceptance validates the original checkpoint identity and digest under the site lock, binds them to the immutable audit, and returns a durable receipt. An exact retry acknowledges the accepted version without rewinding a later head or review status. Changed or missing bases fail before writes. Schema and acceptance regressions failed16/17 before the backport and pass17/17 afterward. The related control, checkpoint, version, endpoint and gateway suite passes62 tests across8 files. Changed-file ESLint and deployment target guard pass. Deployment and authenticated persistence readback are still required.

The browser scrolling fix is separately deployed as Foundation `a2a5401`, staging Worker `b961bca7-8b40-4bb0-8bc2-85afe52745b8`, container image `sha256:63875d7382270c6a809bb8d3d2ba0a5ceb2646ddd687c788867b043efdaeb4cf`. Its actual review is bounded at24–889px in a913px viewport, overflow auto, scrollTop3724.5 at the bottom; Accept is visible and hit-testable. Proposal `proposal_a92026b1-6f36-450f-84f7-1b2c7687f295` adds only the requested form and component. Exact removal restores the previous durable document. No AI acceptance committed: synthetic head remains `checkpoint_label_repair_20260912`, current version null. Temporary allowance restored to0, portal logout200, zero test sessions, three owned tabs closed, and raw login/trace files removed.

Disk pressure interrupted the earlier container image export. Supported Docker restart and authorized unused package/build cache cleanup recovered the build; no source, release images, containers or volumes were deleted. Verify both container and Worker readback: a Worker upload can succeed before an image export fails.

## Signed-session acceptance repair — 12 September 2026

Dashboard preview schema repair `52a27df70d7abcad903d20b4f2fe89acdd241051` is independently verified live as Pages deployment `8c23066e-64ca-4701-a9ad-3957b0cfb2f1` (clean source, preview only). Build passed at 25,120,801 raw bytes; all 62 related tests, lint and deployment guard passed. The historical entries below describe earlier states.

Fresh normal chatbot generation returned proposal `proposal_acae2ab9-f86e-41dd-b1bd-a6a7da0228b8`. Review scrolling and visible Accept passed. Acceptance still returned 502: a service-binding probe isolated upstream 401 `SESSION_REQUIRED`. The preview API correctly requires both machine authentication and the signed active editor session; Sandbox omitted the session and the gateway stripped that header.

The correction relays only the already-verified cookie/Bearer session through the private acceptance client, and the gateway forwards it only on the exact acceptance POST route. Caller-supplied forwarding headers are ignored by Sandbox; credentials never enter model/checkpoint bodies. Existing API authentication remains intact. Regression tests failed before the correction and pass afterward. Foundation full gates: 2,890 tests / 259 files, build 22 tasks, typecheck 36 tasks plus security, lint 913 files. Gateway acceptance tests: 24 passing; lint, TypeScript and staging dry run pass. Sandbox Worker-only staging dry run passes. Deploy both private staging Workers next; preserve the verified `a2a5401` container image.

No AI acceptance committed yet: synthetic head `checkpoint_label_repair_20260912`, current version null. Latest test cleanup: allowance restored to 0, logout 200, zero test sessions, two owned tabs closed, raw login and trace files removed. Production remains Sandbox `22922bdd` and Dashboard PR532. All seven roadmap areas and EDITOR-03 remain open. Next: normal authenticated generation → review → accept → reconnect, then governed synthetic submission. Local Graph Wiki updated; no hosted sync claimed.
