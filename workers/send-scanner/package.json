{
  "name": "dashboard-send-scanner",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types ./src/worker-configuration.d.ts",
    "typecheck": "tsc --noEmit",
    "deploy:dry-run": "node scripts/verify-config.mjs && wrangler deploy --dry-run",
    "deploy": "node scripts/verify-config.mjs && wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "pg": "^8.22.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.112.0"
  }
}
