{
  "name": "google-pmax-provider",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "deploy:dry-run": "WRANGLER_WRITE_LOGS=false wrangler deploy --config wrangler.toml --dry-run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260625.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.99.0"
  }
}
