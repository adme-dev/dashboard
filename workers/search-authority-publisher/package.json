{
  "name": "@xeroflow/search-authority-publisher",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types --config wrangler.jsonc --include-runtime=false worker-configuration.d.ts",
    "typecheck": "pnpm types && tsc --noEmit",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir ../../.output/search-authority-publisher --config wrangler.jsonc"
  }
}
